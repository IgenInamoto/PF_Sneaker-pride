<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>Image</th>
      <th>Explanation </th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% sneakers.each do |sneaker| %>
      <tr>
        <td><%= link_to(sneaker.user) do %>
          <%= image_tag sneaker.user.get_profile_image, size:'50x50' %>
          <% end %>
        </td>
        <td><%= link_to sneaker.image,sneaker, class: "sneaker_#{sneaker.id}" %></td>
        <td><%= sneaker.body %></td>
        <td class=<%#= "favorite-" + book.id.to_s %> >
            <%#= render "favorites/btn", book: book %>
        </td>
        <td><%#= render "book_comments/counter", book: book %></td>
      </tr>
    <% end %>
  </tbody>
</table>