<%= form_with(model:[sneaker, sneaker_comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'3',placeholder: "コメントをここに", class: "w-100 comment-textarea" %>
  <%= f.submit "Send", class: "btn btn-info btn-base-1 mt-10 pull-right" %>
<% end %>